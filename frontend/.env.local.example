# Required: Your Ethereum private key (with 0x prefix)
# WARNING: Never commit real private keys to version control!
ETH_PRIVATE_KEY=0x...

# Optional: RPC URL (defaults to https://eth.llamarpc.com)
RPC_URL=https://eth.llamarpc.com

# Optional: Chain ID (defaults to 1 for Ethereum mainnet)
# Common values: 1=Ethereum, 10=Optimism, 137=Polygon, 42161=Arbitrum, 8453=Base, 11155111=Sepolia
CHAIN_ID=1

# Optional: Stablecoin spending limits in USD (defaults shown)
LIMIT_PER_TX=1000
LIMIT_PER_HOUR=5000
LIMIT_PER_DAY=20000

# Optional: Approval timeout in minutes (defaults to 60)
APPROVAL_TIMEOUT_MINUTES=60
